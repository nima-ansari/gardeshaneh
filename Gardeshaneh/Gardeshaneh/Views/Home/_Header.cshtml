@* @model List<City>
 *@
<header id="site-header" class="py-1 mb-4 border-bottom">
    <div class="container">
        <div class="row p-2 pb-0 pe-lg-0 pt-lg-5 align-items-center text-center rtl">
            <div class="col-lg-12 p-3 p-lg-5 pt-lg-3">
                <h1 class="display-4 fw-bold lh-1">گردشانه</h1>
                <p class="lead">راهنمای پیدا کردن بهترین مکان‌های تفریحی و گردشی </p>
                <form asp-controller="place" asp-action="index" method="get" id="home-search-form" class="rtl">
                    <div class="input-group mb-3" style="top:11px;">
                        <input id="home-input-text"
                               name="Title"
                               type="text"
                               class="form-control"
                               placeholder="مکان مورد نظرتان را جستجو کنید...">
                        <button id="home-search-button"
                                class="btn btn-primary"
                                type="submit">
                            جستجو
                        </button>
                    </div>
                </form>
                <div class="pt-3">
                    <p class="d-inline">شهرها: </p>
                   @*  @if (Model != null && Model.Any())
                    {
                        @foreach (var city in Model)
                        {
                            <a asp-controller="Place"
                               asp-action="Index"
                               asp-route-CityId="@city.Id"
                               title="مکان‌های شهر @city.PersianName"
                               class="btn btn-success bi-geo-alt-fill">
                                @city.PersianName
                            </a>
                        }
                    } *@
                </div>
            </div>
        </div>
    </div>
</header>
